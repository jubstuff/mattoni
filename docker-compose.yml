version: '3.8'

services:
  app:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - budget-data:/app/server/data
    environment:
      - NODE_ENV=production
      - PORT=3001
    restart: unless-stopped

volumes:
  budget-data:
